# CLIP 1a — KF0 → KF0.5  (Office monitor → upload card fills the frame)

> **Mode:** Image-to-video. FIRST FRAME = `images/network-dive/kf0_hero.jpg`. LAST FRAME = `images/network-dive/kf0_5_upload_dissolve.jpg`. Directional dive transition, not a loop. The final frame must resolve exactly to the KF0.5 reference so Clip 1b continues seamlessly.

### SCENE
- Opening: dark corporate office at night, over-the-shoulder view of an anonymous employee, widescreen monitor centered showing a Claude-style AI chat interface with a file-attachment card mid-upload (KF0).
- Closing: extreme close-up — the upload card ('Q3_performance_report_2024.xlsx', coral progress bar, 'Uploading') fills the frame, RGB subpixel lattice emerging through the cream UI, the card's corner beginning to dissolve into tiny cyan pixel particles (KF0.5).
- Mood references: Apple product-film opening push-in; Fincher-grade restraint.
- Color palette: warm off-white / cream UI glow dominant throughout; the first traces of electric cyan appear only near the end at the dissolving corner.

### CAMERA
- Single continuous forward dolly push-in along the Z-axis, dead-straight into the upload card at the center of the monitor. No pan, no roll.
- Optics graduate from a 35mm over-the-shoulder framing to a 60mm macro framing on the card.
- Start pose = KF0 exactly. End pose = KF0.5 exactly.
- Speed: smooth near-linear velocity — scroll-scrubbed clip, no aggressive easing.
- The employee's silhouette slides out of the lower-left frame early; from ~40% on, only the screen exists.

### ACTION ARC
- **0–40%:** camera accelerates over the shoulder toward the screen; the room's edges and silhouette exit the frame; the monitor grows to fill everything. On the screen, the progress bar visibly advances.
- **40–75%:** the UI enlarges past readable scale — the upload card becomes the whole world. Glass micro-texture, anti-glare grain and faint moiré resolve into view.
- **~75% — PEAK MOMENT:** the RGB subpixel lattice fades up through the cream interface — the first reveal that this "document" is physically just glowing dots.
- **75–100%:** the card's near corner begins to break apart: the first tiny cyan pixel particles detach and drift. Frame settles exactly into KF0.5.

### TEXT
No baked headline. Web overlay for this beat: *"What happens when a file leaves your company?"* — upper third, over the dark office region early in the clip. The diegetic UI text (filename, 'Uploading') is part of the plate and must stay legible.

### LIGHTING & ATMOSPHERE
- Single source: the monitor. Warm off-white throughout; no color shifts — the cyan traces at the end are scene elements (particles), not a grade change.
- Faint dust motes in the screen glow early; glass glare and moiré shimmer late. No flicker, no exposure pumping.

### CLIP SEAM (continuity lock)
- First frame pixel-identical to KF0 render; last frame pixel-identical to KF0.5 render.
- One-way forward travel. No loop.

### TECHNICAL
- Duration: 5 seconds
- Directional image-to-video (different first and last frames)
- Even, scrub-friendly motion
- No watermarks
- 4K resolution, high frame rate

# CLIP 5 — KF4 → KF5  (Proxy scan → the file is identified, magic-number match)

> **Seedance mode:** Image-to-video. FIRST FRAME = `images/network-dive/kf4_proxy.jpg`. LAST FRAME = `images/network-dive/kf5_detected.jpg`. Directional dive transition, not a loop. Final frame must resolve exactly to the KF5 reference so Clip 6 continues seamlessly.

### SCENE
- Opening: the two-faced gateway inspecting the passing stream with a cyan scan grid, wireframe brackets on packet-blocks (KF4).
- Closing: a close-up inside the inspection chamber — a single extracted packet-block held in a cyan wireframe bracket, its surface unfolded to reveal rows of hex/binary, a warm-amber magic-number signature highlighted, and a legible `FILE DETECTED` readout (KF5).
- Mood references: the forensic "we found it" beat of a tech thriller; Apple/Nvidia macro-detail cutaways. Photoreal, disciplined, legible.
- Color palette: deep near-black, electric cyan (wireframe/scan) dominant, warm amber alert accent on the matched signature.

### CAMERA
- A controlled push-in and selection: from the wide hero framing of the gateway, the camera moves in on ONE bracketed packet-block, which lifts out of the stream toward the lens as the surroundings fall into shadow.
- Effective optics tighten from 35mm to a 50mm feel — intimate but not distorted.
- Start pose = KF4 exactly. End pose = KF5 exactly (single packet-block centered, unfolded, readout to the side).
- Speed: smooth, deliberate, near-linear. Slower and more precise than the flythrough clips — this is a "focus and read" moment.
- Depth of field shallow; the selected packet-block and readout sharp, background streams soft.

### ACTION ARC
- **0–30%:** among the many bracketed packets, one is chosen — its wireframe brightens and it separates from the stream, drawn toward the camera. The rest dim into the background.
- **30–60%:** the packet-block's surface unfolds, revealing layered content: rows of raw hexadecimal bytes and binary digits. A thin clean scan line reads across the rows.
- **~60% — PEAK MOMENT:** at the leading edge, a small cluster of bytes ignites in warm amber — the **magic-number (file signature) match**. This is the identification: the proxy confirming the true file type from raw bytes, regardless of a claimed extension.
- **60–100%:** a minimal holographic readout resolves beside the packet, its short monospaced text settling into crisp legibility: `FILE DETECTED` with a small file-type tag. Settles into KF5's composition.

### TEXT
Two layers of text here — keep them distinct:
- **In-scene (baked, diegetic):** the `FILE DETECTED` readout and the hex/byte rows are part of the shot. Keep them short and crisp; regenerate if characters garble.
- **Web overlay headline:** *"We see the file. Extension can't lie."* — positioned opposite the diegetic readout so the two never overlap. Over a dark region.

### LIGHTING & ATMOSPHERE
- Cyan wireframe glow is the key; warm amber is the single alert accent on the signature match. Deep shadow surround.
- Static grade — the only change in warmth is the localized amber ignition of the signature, which is a scene element, not a global light shift.
- Atmosphere: faint particles, gentle volumetric glow. Calm, precise, low motion.
- No lens flare spam, no flicker.

### CLIP SEAM (continuity lock)
- First frame pixel-identical to KF4 render; last frame pixel-identical to KF5 render.
- End state (packet-block pose, unfolded byte rows, amber signature, readout text, grade) must match KF5 so Clip 6 starts with no jump.
- One-way travel. No loop.

### TECHNICAL
- Duration: 5 seconds
- Directional image-to-video (different first and last frames)
- Even, scrub-friendly motion; slower, precise pacing
- No watermarks
- 4K resolution, high frame rate
